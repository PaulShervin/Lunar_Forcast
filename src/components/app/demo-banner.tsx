/**
 * DemoBanner
 * ─────────────────────────────────────────────────────────────────────────────
 * Sticky amber banner shown at the top of every page when Demo Mode is active.
 * Clearly communicates that real backend data is bypassed.
 *
 * TO REMOVE DEMO MODE: delete this file and its import in _app.tsx.
 * ─────────────────────────────────────────────────────────────────────────────
 */

import { FlaskConical, X } from "lucide-react";
import { useDemoStore } from "@/store/demo-store";

export function DemoBanner() {
  return null;
}
