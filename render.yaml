services:
  - type: web
    name: lunar-forecast
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_DEMO_MODE
        value: "true"
